FROM film
WHERE title LIKE '%n'
ORDER BY length DESC
FETCH FIRST 5 ROWS ONLY;

SELECT *
FROM film
WHERE title LIKE '%n'
ORDER BY length ASC
OFFSET 1
FETCH FIRST 5 ROWS ONLY;

SELECT *
FROM customer
WHERE store_id = 1
ORDER BY last_name DESC
FETCH FIRST 4 ROWS ONLY;